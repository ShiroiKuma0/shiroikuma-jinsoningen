<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.RUN_USER_INITIATED_JOBS" />
    <uses-permission android:name="android.permission.UPDATE_PACKAGES_WITHOUT_USER_ACTION" />
    <uses-permission android:name="android.permission.ENFORCE_UPDATE_OWNERSHIP" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <!-- shiroikuma fork: the 保存復元 contract lets 白い熊 自由作業盤 pass an absolute `path` to
         export into, which needs All-Files-Access. Declaring it is not holding it, so the export
         tests Environment.isExternalStorageManager() first and answers ERROR:no-storage-access
         rather than failing halfway. The Export/Import panel's own SAF folder needs none of this. -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <!-- shiroikuma fork: 白い熊 雫 (shiroikuma.shizuku) grants its clients
         af.shizuku.plus.permission.API_V23 rather than the stock
         moe.shizuku.manager.permission.API_V23, which it cannot define while installed beside
         stock Shizuku. The Shizuku API library merges the stock one in for us; this is the other
         half, so our own Shizuku can grant us too. -->
    <uses-permission android:name="af.shizuku.plus.permission.API_V23" />

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".Droidify"
        android:allowBackup="true"
        android:banner="@drawable/tv_banner"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/application_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/MainTheme"
        tools:ignore="UnusedAttribute">

        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>

        </receiver>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.SHOW_APP_INFO" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="fdroid.app" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="details"
                    android:scheme="market" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="search"
                    android:scheme="market" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="f-droid.org" />
                <data android:host="www.f-droid.org" />
                <data android:host="staging.f-droid.org" />
                <data android:pathPattern="/app/.*" />
                <data android:pathPattern="/packages/.*" />
                <data android:pathPattern="/.*/packages/.*" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="apt.izzysoft.de" />
                <data android:pathPattern="/fdroid/index/apk/.*" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <data android:host="droidify.eu.org" />
                <data android:pathPattern="/app/.*" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <data android:host="droidify.app" />
                <data android:pathPattern="/app/.*" />
            </intent-filter>

            <!--Adding repo with special url-->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="fdroidrepo" />
                <data android:scheme="fdroidrepos" />
            </intent-filter>

        </activity>

        <service
            android:name=".service.SyncService"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".service.SyncService$Job"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <service
            android:name=".service.DownloadService"
            android:foregroundServiceType="dataSync" />

        <!-- shiroikuma fork: the 保存復元 automation contract. The receiver does no work — it
             checks the switch and the token, answers LIST_CATEGORIES inline and hands
             EXPORT_STATE to the foreground service, because a manifest receiver that overruns
             the broadcast window gets the app ANR'd and killed mid-write. No android:permission:
             the caller cannot hold one, so the token is the gate. -->
        <receiver
            android:name=".jinsoningen.automation.StateExportReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="shiroikuma.jinsoningen.action.EXPORT_STATE" />
                <action android:name="shiroikuma.jinsoningen.action.LIST_CATEGORIES" />
                <action android:name="shiroikuma.jinsoningen.action.CANCEL_EXPORT" />
            </intent-filter>
        </receiver>

        <service
            android:name=".jinsoningen.automation.StateExportService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".installer.installers.session.SessionInstallerReceiver"
            android:exported="false" />

        <receiver
            android:name=".receivers.CopyErrorReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.looker.droidify.intent.action.COPY_ERROR" />
            </intent-filter>
        </receiver>

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <provider
            android:name=".utility.common.cache.Cache$Provider"
            android:authorities="${applicationId}.provider.cache"
            android:exported="false"
            android:grantUriPermissions="true" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="remove" />

        <receiver
            android:name=".receivers.UnarchivePackageReceiver"
            android:exported="false"
            tools:targetApi="35">
            <intent-filter>
                <action android:name="android.intent.action.UNARCHIVE_PACKAGE" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
